// const names = ["Fadhil", "Doraemon", "Lingga"];

// names.splice(1, 1);// kegunaan untuk menghapus mulai dari data ke berapa, namun (1, 1) mulai dari berapa dan berapa item yg ingin dihapus
// names.reverse(); // untuk memutabalik data yg ingin ditampilkan  

// names[1] = "Roji"; // mengganti data berdasarkan tempat arraynya 

const grid = [
    [1, 2, 3], //0
    [4, 5, 6], //1
    [7, 8, 9], //2
];

const training = ["Enigma", "Camp", "Code", "Node"];
let size = training.length;

const name1 = ["Cecilie", "Lone"];
const name2 = ["Emil", "Tobias", "Linus"];
const name3 = ["Robin", "Morgan"];
const students = name1.concat(name2, name3);

console.log(students); // [2] memanggil array ke brp, [1] memanggil index ke brp diarray yg kita panggil hasilnya = 8

